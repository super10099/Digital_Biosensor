package com.example.myapplication;

import org.junit.Test;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertTrue;

import java.util.*;
import android.graphics.Color;

/**
 * Test cases for DataProcessing class.
 */
public class DataProcessingTest {
    @Test
    public void addition_isCorrect() {
        assertTrue(true);
    }

    @Test
    public void getRPointTest() {
        List<Color> sampleColors = new ArrayList<Color>();

//        sampleColors.add(Color.valueOf(255, 255, 255));
//        sampleColors.add(Color.valueOf(0, 0, 0));
//        sampleColors.add(Color.valueOf(50 ,210, 192));
//        sampleColors.add(Color.valueOf(120, 90, 52));

//        List<Double> rPointsAnswers = new ArrayList<Double>();
//        rPointsAnswers.add( (255d+255d) / 255d);
//        rPointsAnswers.add( (0d+0d) / 0d);
//        rPointsAnswers.add( (50d+210d) / 192d);
//        rPointsAnswers.add( (120d+90d) / 52d);
//
//        DataProcessor dp = new DataProcessor(sampleColors);
//        dp.start();
//        List<Double> rPoints = dp.getRPoints();
        //Log.d()
//        assertEquals(rPoints.get(0), rPointsAnswers.get(0), 0.1);
//        assertEquals(rPoints.get(1), rPointsAnswers.get(1), 0.1);
//        assertEquals(rPoints.get(2), rPointsAnswers.get(2), 0.1);
//        assertEquals(rPoints.get(3), rPointsAnswers.get(3), 0.1);
    }
}
